#include <stdio.h>

// 3-Seja o seguinte trecho de código:

int a = 10, b = 5;
int *x, *y;
x = &a;
y = &b;

// Determine o resultado das seguintes expressões:

/*
a) x == &a R: verdadeiro, eles são iguais.
b) *x + *y  R: 15.
c) *&y R: *&y é o mesmo que y: resultado = &b.
d) *x * *y - a R: 40.
*/


